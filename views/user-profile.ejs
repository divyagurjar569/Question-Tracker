<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        /* General styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            color: #333;
            margin: 0;
            padding: 0;
            transition: background-color 0.3s ease;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            animation: fadeIn 0.5s ease forwards;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #5c67a3;
            transition: color 0.3s ease;
        }

        .profile-details {
            margin-bottom: 20px;
        }

        .profile-details p {
            font-size: 1.1em;
            margin-bottom: 10px;
        }

        /* Button styles */
        .button-container {
            text-align: center;
        }

        .back-button,
        .edit-button {
            display: inline-block;
            padding: 10px 20px;
            margin: 0 10px;
            text-decoration: none;
            color: #fff;
            background-color: #5c67a3;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            box-shadow: 0 5px 15px rgba(92, 103, 163, 0.3);
        }

        .back-button:hover,
        .edit-button:hover {
            background-color: #44508c;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .container,
        h1,
        .profile-details p {
            animation: slideIn 0.5s ease forwards;
            animation-delay: 0.2s;
        }

        .back-button,
        .edit-button {
            animation: slideIn 0.5s ease forwards;
            animation-delay: 0.4s;
        }

        /* Hover effects */
        .back-button:hover,
        .edit-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(92, 103, 163, 0.4);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>User Profile</h1>
        <div class="profile-details">
            <p><strong>Username:</strong> <%= user.username %></p>
            <p><strong>Email:</strong> <%= user.email %></p>
            <!-- Add more details here as needed -->
        </div>
        <!-- Add a back button to navigate back to the profile page -->
        <div class="button-container">
            <a href="/profile" class="back-button">Back to Profile</a>
            <a href="/edit-profile" class="edit-button">Edit Profile</a>
        </div>
    </div>
</body>
</html>
